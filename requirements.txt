# Core dependencies for the Jira CSV Manager
# Install with: pip install -r requirements.txt

jira>=3.0.0
python-dotenv>=0.19.0

# Optional: For better CSV handling (the script works with built-in csv module too)
# pandas>=2.0.0